services:
  wggbot:
    container_name: wggbot
    build: .
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - LANG=en_US.UTF-8
      - LC_ALL=en_US.UTF-8
    command: >
      bash -c "
        echo '🚀 Starting bot...' &&
        tail -f /dev/null
      "
    restart: unless-stopped
